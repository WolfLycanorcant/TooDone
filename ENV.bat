#If the environment is the default environemnt created, as venv, double clicking and running this on a windows machine will enter the environment with the python dependencies required. 

python -m venv venv
CALL .\venv\Scripts\activate
cmd
